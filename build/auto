#!/usr/bin/env zsh

os=`uname -o`
action=$1

print "$action on $os"
case $os in
    Cygwin)
        print "Windows $action"
        ./build/${action}-win.sh
        ;;
    GNU/Linux)
        print "GNU/Linux $action"
        ./build/${action}-gnu.sh
        ;;
esac

